# projectM — Agent 入口

个人媒体记录站。先读 `docs/PLAN.md`，再按其中「实现顺序」动手，不要另起一套产品定义。

## 做什么

私人豆瓣：记录我玩过的游戏、读过的书、看过的电影。

- 搜索外部资料 → 入库一份快照 → 自己打分、写短评、标状态
- 类型可扩展（先 movie / book / game，以后可加 tv 等）
- 个人自用，单用户即可，不必做账号体系

## 不要做什么

- **禁止爬取或逆向**豆瓣、小黑盒或任何未授权站点（含抓包、签名、Cookie、非官方转发 API）
- 不要镜像对方整库；只存搜索选中的条目快照 + 用户自己的记录
- 不要改成社交网络、推荐算法、微服务
- 不要和别的仓库（例如嵌入式固件工程）混在一起

## 数据从哪来

| 类型 | Provider | 说明 |
|------|----------|------|
| 电影 | TMDB | 中文名、海报、年份 |
| 书 | Google Books（首选）或 Open Library | 书名 / ISBN |
| 游戏 | Steam | 中文名尽量带上 |

Key 放环境变量，不要提交进 git。

## 技术选型（已定，不要换）

- Next.js（App Router）+ TypeScript
- SQLite（Prisma 或 drizzle，二选一后保持）
- 服务端拉外部 API，浏览器不直连第三方（避免漏 Key）

## 实现时

1. 先数据模型（`Item` + `Entry`），再 Provider 接口，再页面
2. 新类型 = 新 Provider，不要复制一套「电影表 / 书表 / 游戏表」
3. 简单直接：能一个文件说清的不要拆三层抽象
4. 改代码前对照 `docs/PLAN.md` 的当前阶段；做完更新「当前进度」

详细产品、表结构、页面、分期见 **`docs/PLAN.md`**。
